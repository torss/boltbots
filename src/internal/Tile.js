export class Tile {
    constructor(x, y, orientation, type, theme) {
        this.x = x;
        this.y = y;
        this.orientation = orientation;
        this.type = type;
        this.theme = theme;
    }
}